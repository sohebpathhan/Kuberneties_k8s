apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-dep
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-dep

  strategy:
    type: RollingUpdate

  template:
    metadata:
      labels:
        app: my-dep

    spec:
      containers:
      - name: nginx-count
        image: nginx
        ports:
        - containerPort: 80

      - name: httpd-count
        image: httpd
        ports:
        - containerPort: 81

---
apiVersion: v1
kind: Service
metadata:
  name: my-service

spec:
  selector:
    app: my-dep

  ports:
  - name: nginx-port
    protocol: TCP
    port: 80
    targetPort: 80

  - name: httpd-port
    protocol: TCP
    port: 81
    targetPort: 81

  type: NodePort